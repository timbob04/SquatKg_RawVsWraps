The main script is `main.m`, which loads the data, processes it, and runs the analyses.

The file paths and data source at the top of `main.m` will need to be updated before running.

After running `main.m `, the figures are generated using the files named as Fig_1..., etc.

The paper is [here](SquatKg_Raw_vs_Wraps.pdf).
